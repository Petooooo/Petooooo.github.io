---
import { getCollection } from "astro:content";
import BaseLayout from "@/layouts/BaseLayout.astro";
import ContentCard from "@/components/ContentCard.astro";
import SectionHeader from "@/components/SectionHeader.astro";
import { byDateDesc, getCollectionUrl, isPublishedEntry } from "@/lib/content";
const entries = (await getCollection("devops")).filter(isPublishedEntry).sort(byDateDesc);
---
<BaseLayout title="DevOps Logs" description="Infrastructure operation journal.">
  <section class="py-14">
    <SectionHeader eyebrow="Operations journal" title="DevOps Logs" description="Infrastructure changes, incidents, maintenance routines, and deployment notes from self-hosted systems." />
    <div class="grid gap-5 md:grid-cols-2">{entries.map((entry) => <ContentCard entry={entry} href={getCollectionUrl(entry)} label="devops" />)}</div>
  </section>
</BaseLayout>
